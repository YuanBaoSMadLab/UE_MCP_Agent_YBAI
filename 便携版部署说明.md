# YB-AI 便携版部署说明

## 什么是便携版？

便携版是一个不需要安装 Node.js 的完整解决方案，你可以将整个文件夹复制到任何 Windows 电脑上直接运行！

## 部署步骤

### 1. 在开发机上打包项目

在原项目目录下运行：

```bash
npm run build
```

### 2. 创建便携版文件夹

在你喜欢的位置创建一个文件夹（例如 `YB-AI-Portable`），然后将以下文件/目录复制进去：

```
YB-AI-Portable/
├── YB-AI启动.bat          <-- 刚才创建的启动脚本
├── node.exe               <-- Node.js 便携版（见下方获取方式）
├── dist/
│   └── cli.js
└── templates/             <-- 整个目录复制
    ├── prompts/
    ├── skills/
    └── tools/
```

### 3. 获取 Node.js 便携版

下载地址：https://nodejs.org/dist/v24.x/

下载：`node-v24.x.x-win-x64.zip`

解压后，在解压目录中找到 `node.exe`，复制到便携版文件夹即可！

### 4. 运行

双击 `YB-AI启动.bat` 即可！

## 与 UE 对接提示

1. 将整个便携版文件夹放在 UE 项目的某个位置
2. 通过 UE 的命令行功能调用 `YB-AI启动.bat`
3. 所有配置和日志仍然保存在用户目录 `~/.deepcode/` 下，不会影响便携版文件夹的移动

## 注意事项

- 确保 `node.exe`、`dist/` 和 `templates/` 都在同一个目录下
- 首次运行时会自动创建 `~/.deepcode/` 配置目录
